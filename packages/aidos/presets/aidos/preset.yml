# The aidos preset display metadata. Display text only: the id is the
# directory name and the trust comes from the root the preset was
# discovered under.
name: Aidos
description: The ticket board agent: plan, tickets, evidence, and state-gated tool access.
order: 50
